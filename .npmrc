# Configuración para npm workspaces
# Hoistear todas las dependencias al root (más simple para monorepos)
# IMPORTANTE: Al instalar, usar --include=dev para instalar devDependencies
shamefully-hoist=true
